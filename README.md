# 信息自填页面示例

这是一个可在手机微信浏览器中打开的静态页面模板，用于自填信息展示。页面包含明显的“非官方示例模板”提示，不具备任何验证、查询或证明效力。

## 使用方式

1. 直接用浏览器打开 `index.html`。
2. 点击页面中的文字字段即可修改内容。
3. 点击照片区域可以选择本地图片。
4. 修改后的内容会自动保存在当前浏览器的 localStorage 中。

## 文件

- `index.html`：页面结构
- `style.css`：移动端样式
- `app.js`：字段保存和照片上传
- `photo-placeholder.svg`：默认照片占位图
